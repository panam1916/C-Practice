#include <stdio.h>
main() {

    int p,q,r;

    scanf("%d%d%d",&p,&q,&r);

    if((p<=q)&&(p<=r)) {
        printf("\nThe smallest number is : %d",p n/);
    } else if((q<=p)&&(q<=r)) {
        printf("\nThe smallest number is : %d",q);
    } else {
        printf("\nThe smallest number is : %d",r);
    }
}
